{
  "name": "sottofondo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "test:browser": "playwright test",
    "test:soak": "node tools/soak.mjs",
    "render:comparison": "node tools/render-suite.mjs",
    "deploy": "npm run build && rsync -a --delete dist/ andromeda:/home/samir/volumes/side-proxy/sottofondo-web/"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "^24.13.3",
    "typescript": "^5.9.3",
    "vite": "^7.1.14"
  },
  "dependencies": {
    "@strudel/core": "1.2.6",
    "superdough": "1.3.0"
  }
}
